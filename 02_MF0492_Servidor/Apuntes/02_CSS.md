# CSS — selectores y layout

- Selectores básicos (`.clase`, `#id`, `elemento`)
- **Flexbox** y **Grid** para layout.

```css
.container { display: flex; gap: 1rem; }
.item { border: 1px solid #ccc; padding: .5rem; }
```
